<h1>Books</h1>

<ul>
  <% @books.each do |book| %>
    <li><%= link_to book.title, book_path(book) %>, <%= book.published %></li>

  <% end %>
</ul>

<%= link_to "Alphabetize", books_alphabetize_path %>

<%= link_to "New Book", new_book_path %>
